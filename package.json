{
  "name": "easycursors",
  "version": "1.0.0",
  "description": "A tool for creating .INF and index.theme custom cursor installation and setup files for Windows and Linux.",
  "main": ".eleventy.js",
  "scripts": {
    "clean": "rimraf _public",
    "go": "npx @11ty/eleventy --serve",
    "go-x": "npm run clean && npm run go",
    "build": "npm run clean && npx @11ty/eleventy"
  },
  "author": "Salty.FYI",
  "license": "MIT",
  "dependencies": {
    "split.js": "^1.6.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "rimraf": "^6.1.3"
  }
}
